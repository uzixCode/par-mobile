import 'package:flutter/material.dart';

class RiwayatSUOPage extends StatelessWidget {
  const RiwayatSUOPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}