import 'package:flutter/material.dart';

class AbsensiDriverPage extends StatelessWidget {
  const AbsensiDriverPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Absensi"),
      ),
    );
  }
}
