import 'package:flutter/material.dart';

class PerjalananDriverPage extends StatelessWidget {
  const PerjalananDriverPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Perjalanan"),
      ),
    );
  }
}
