import 'package:flutter/material.dart';
import 'package:par_mobile/views/manager/riwayat/components/body.dart';

class RiwayatManagerPage extends StatelessWidget {
  const RiwayatManagerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
