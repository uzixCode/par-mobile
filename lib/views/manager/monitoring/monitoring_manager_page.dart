import 'package:flutter/material.dart';
import 'package:par_mobile/views/manager/monitoring/components/body.dart';

class MonitoringManagerPage extends StatelessWidget {
  const MonitoringManagerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
