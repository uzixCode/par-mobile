import 'package:flutter/material.dart';
import 'package:par_mobile/views/manager/approval/components/body.dart';

class ApprovalManagerPage extends StatelessWidget {
  const ApprovalManagerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Body());
  }
}
